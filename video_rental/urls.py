__author__ = 'CRogers'
from django.conf.urls import patterns, url
from video_rental import views
# urlpatterns = patterns('',
#                        # url(r'^list/$', views.VideoListView.as_view(), name="list"),
#                        # url(r'^search/$', views.search, name="search"),
#                        # url(r'(?P<pk>\d+)/info/$', views.VideoView.as_view(), name="info"),
#                        # url(r'^(?P<video_id\d+)/rent/$',views.rent, name="rent"),
#                        # url(r'(^/$)|(^home/$)',views.home, name="home"),
#
# )